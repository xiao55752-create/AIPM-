# DESIGN.md · AI 产品成长营

本文件是本项目的视觉与交互单一事实源（Single Source of Truth）。  
所有页面设计、组件开发、文案排版应优先遵循本文件。

## 1) 品牌定位与气质

- **定位**：AI 产品能力成长操作系统（不是资讯站、不是泛课程站）
- **气质关键词**：专业、可信、克制、结构化、结果导向
- **避免**：泛紫色 AI 渐变模板感、过度装饰、强营销夸张文案

## 2) 视觉基调

- **默认风格**：浅色高对比 + 轻玻璃感卡片
- **信息密度**：中高密度，使用 Bento 网格组织复杂信息
- **信任优先**：边框、层级、数据证据先于炫技动效

## 3) 色彩系统（语义角色）

- `--color-primary: #1E3A5F`（品牌主色、标题、深色按钮）
- `--color-accent: #0D9488`（主 CTA、关键状态）
- `--color-accent-hover: #0F766E`
- `--color-accent-muted: #CCFBF1`（弱强调背景）
- `--color-bg: #F8FAFC`（页面背景）
- `--color-surface: #FFFFFF`（卡片背景）
- `--color-text: #0F172A`（正文主文本）
- `--color-text-muted: #64748B`（辅助文本）
- `--color-border: #E2E8F0`
- `--color-video-badge: #FB7299`（B 站标签）

## 4) 排版系统

- 中文优先，标题简短，避免长段堆叠
- 标题层级：
  - `H1`: 32-40 / 700
  - `H2`: 22-28 / 650
  - `H3`: 16-18 / 600
  - 正文：14-16 / 400-500
- 数据数字可适当增大（24-40）以形成“证据感”

## 5) 间距与圆角

- 间距采用 4pt 体系（4/8/12/16/24/32/48）
- 圆角：
  - 常规卡片：`12px`
  - 强调卡片：`16px`
  - 按钮：`10px`
- 边框优先 `1px`，避免阴影过重

## 6) 组件规则

- **按钮**
  - Primary：实心 `accent`
  - Secondary：描边 `primary`
  - Tertiary：文字按钮（带 hover 下划线）
- **卡片**
  - 默认：白底 + 1px 边框
  - 强调：浅品牌底色 + 边框
- **标签（Chips）**
  - 灰底信息标签、品牌色强调标签并存
- **数据块**
  - 使用“标题 + 数值 + 解释”三层结构

## 7) 交互与动效

- 动效只服务反馈，不做炫技
- hover/active 变化应在 150-250ms 内
- 关键反馈必须可见：
  - 保存成功
  - 复制成功
  - 导出完成
  - 回填确认

## 8) 页面结构模板（首页）

1. Hero：价值主张 + 单一主 CTA
2. 信任条：数据或标志（社会证明）
3. 核心流程：测 -> 走 -> 学 -> 用（Bento）
4. 产品演示：流程化演示而非静态截图
5. 招聘对标价值：学习到求职/晋升闭环
6. 案例证据：脱敏案例 + 量化结果
7. 最终 CTA：自测或进入成长营

## 9) 可访问性

- 文本与背景对比符合 WCAG AA
- 所有交互元素需有 `focus-visible`
- 移动端最小可点击区域 >= 40px
- 不仅靠颜色表达状态（需配文案或图标）

## 10) 开发执行约束

- 新 UI 先遵循本文件，不临时发明风格
- 新增组件需可复用，不写一次性样式
- 每次改版至少检查：
  - 构建可通过
  - 移动端可用
  - 关键流程可走通
